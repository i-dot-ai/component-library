// Minimal Svelte config so tooling (svelte-check / the VS Code Svelte extension)
// resolves these as Svelte 5 components. No preprocessor: components are plain
// Svelte 5 (runes, {@render}) with no TS or SCSS in the .svelte files.
export default {};
