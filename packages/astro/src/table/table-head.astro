---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-table__head", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<thead class={classes} {...rest}>
    <slot />
</thead>
