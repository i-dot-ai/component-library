---
const {
    inline = false,
    small = false,
    class: className,
    ...rest
} = Astro.props;

const classes = [
    "govuk-radios",
    inline ? "govuk-radios--inline" : "",
    small ? "govuk-radios--small" : "",
    className ?? "",
]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} data-module="govuk-radios" {...rest}>
    <slot />
</div>
