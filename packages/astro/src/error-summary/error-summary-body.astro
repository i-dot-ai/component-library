---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-error-summary__body", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} {...rest}>
    <slot />
</div>
