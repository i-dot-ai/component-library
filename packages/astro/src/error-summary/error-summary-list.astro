---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-list govuk-error-summary__list", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<ul class={classes} {...rest}>
    <slot />
</ul>
