---
const { class: className, ...rest } = Astro.props;

const classes = ["iai-card__heading", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} {...rest}>
    <slot />
</div>
