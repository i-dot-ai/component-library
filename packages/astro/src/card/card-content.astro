---
const { class: className, ...rest } = Astro.props;

const classes = ["iai-card__content", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} {...rest}>
    <slot />
</div>
