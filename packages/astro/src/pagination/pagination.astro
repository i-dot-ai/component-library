---
// AUTO-GENERATED from HTML spec. Do not edit by hand.
const { block = false, class: className, ...rest } = Astro.props;

const classes = [
    "govuk-pagination",
    block ? "govuk-pagination--block" : "",
    className ?? "",
]
    .filter(Boolean)
    .join(" ");
---

<nav class={classes} aria-label="Pagination" {...rest}>
    <slot />
</nav>
