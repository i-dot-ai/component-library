---
const { href, class: className, ...rest } = Astro.props;

const classes = ["govuk-breadcrumbs__list-item", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

{
    href === undefined ? (
        <li class={classes} aria-current="page" {...rest}>
            <slot />
        </li>
    ) : (
        <li class={classes} {...rest}>
            <a class="govuk-breadcrumbs__link" href={href}>
                <slot />
            </a>
        </li>
    )
}
