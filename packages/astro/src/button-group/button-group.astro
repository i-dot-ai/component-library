---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-button-group", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} {...rest}>
    <slot />
</div>
