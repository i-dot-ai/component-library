---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-phase-banner", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} {...rest}>
    <p class="govuk-phase-banner__content">
        <slot />
    </p>
</div>
