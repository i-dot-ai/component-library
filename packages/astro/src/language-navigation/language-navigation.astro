---
const { ariaLabel = "Language", class: className, ...rest } = Astro.props;

const classes = ["govuk-language-navigation", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<nav class={classes} aria-label={ariaLabel} {...rest}>
    <ul class="govuk-language-navigation__list">
        <slot />
    </ul>
</nav>
