---
const { class: className, ...rest } = Astro.props;

const classes = ["side-nav__subnav-list", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<ul class={classes} {...rest}>
    <slot />
</ul>
