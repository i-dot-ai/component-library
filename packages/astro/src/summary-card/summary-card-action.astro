---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-summary-card__action", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<li class={classes} {...rest}>
    <slot />
</li>
