---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-summary-card__title", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<h2 class={classes} {...rest}>
    <slot />
</h2>
