---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-summary-card__title-wrapper", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} {...rest}>
    <slot />
</div>
