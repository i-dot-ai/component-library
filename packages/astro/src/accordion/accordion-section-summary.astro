---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-accordion__section-summary govuk-body", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} {...rest}>
    <slot />
</div>
