---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-accordion__section", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} {...rest}>
    <slot />
</div>
