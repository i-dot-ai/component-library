---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-file-upload", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<input class={classes} type="file" {...rest} />
