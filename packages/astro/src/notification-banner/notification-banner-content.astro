---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-notification-banner__content", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} {...rest}>
    <slot />
</div>
