---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-notification-banner__heading", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<p class={classes} {...rest}>
    <slot />
</p>
