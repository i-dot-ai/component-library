---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-notification-banner__header", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes} {...rest}>
    <slot />
</div>
