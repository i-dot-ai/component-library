---
const { class: className } = Astro.props;

const classes = ["govuk-checkboxes__divider", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<div class={classes}>
    <slot>or</slot>
</div>
