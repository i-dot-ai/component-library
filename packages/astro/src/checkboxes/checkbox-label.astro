---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-label govuk-checkboxes__label", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<label class={classes} {...rest}>
    <slot />
</label>
