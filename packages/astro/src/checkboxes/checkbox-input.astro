---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-checkboxes__input", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<input class={classes} type="checkbox" {...rest} />
