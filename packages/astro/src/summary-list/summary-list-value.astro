---
const { class: className, ...rest } = Astro.props;

const classes = ["govuk-summary-list__value", className ?? ""]
    .filter(Boolean)
    .join(" ");
---

<dd class={classes} {...rest}>
    <slot />
</dd>
